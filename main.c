#include <stdio.h>

int main(int arg, char* argv[])
{
	if(argc, < 2)
	{
		printf("Usage: %s <flags> <command>\n", argv[0]);
		return 0;
	}

	if(argv[i]) == "--open" || argv[|] == "-0" )
	{
		printf("Is opened!");
		return 0;
	}

	return 0;  
}
